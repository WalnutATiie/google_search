Prerequisite:
    - install python 'gevent' and 'requests' with '[sudo] pip install' or '[sudo] easy_install'

Run:
    1. You can run example.py in this directory
    2. To run example_proxy.py, copy it to the parent folder, and git clone proxymanager.git
